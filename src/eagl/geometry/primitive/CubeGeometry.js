
define(
  [
    '../Geometry',
    '../GeomBuffer'
  ],
  function(
    Geometry,
    GeomBuffer
  ){

  function cube( sx, sy, sz, segx, segy, segz ){

    var geometry = new Geometry();

    segx = (segx === undefined) ? 1 : segx;
    segy = (segy === undefined) ? 1 : segy;
    segz = (segz === undefined) ? 1 : segz;




  }

});